specVersion: "v1beta"
serviceId: "mosaic-waiting"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mosaic-waiting-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
